package com.company;

public class dd_lec_16_ifelse {
    public static void main(String[] args) {
        int age = 16;
        if(age>18)
            System.out.println("yes");
        // if there is only one statement is inside the if condition then there is no need of bracket
        // and if there is more then one statement inside if then we need to use curly bracket
        // if ke andr sirf conditional and logical statement hi use kar sakte hai
        else {
            System.out.println("no");
        }
    }
}
